# Topology Collapsible Filters

## Goal

Reduce initial topology-console vertical space by collapsing filter controls until
a user requests them.

## Tasks

1. Add a route test that verifies filters are collapsed initially and can be
   expanded with an accessible control.
2. Add the collapsed state and disclosure control without changing filtering
   behavior or visible filter counts.
3. Verify frontend checks and update topology documentation if needed.

## Completion

Move this plan to `../completed/` with a `[COMPLETED]` prefix after its tests
and verification commands pass.
